import { Text, View } from 'react-native';

export default function CategoryStep() {
  return (
    <View className="flex size-full flex-col  items-center justify-center bg-green-800">
      <Text className="text-xl font-semibold text-white">Categorias</Text>
    </View>
  );
}
