import { Text, View } from 'react-native';

export default function UserStep() {
  return (
    <View className="flex size-full flex-col  items-center justify-center bg-red-950">
      <Text className="text-xl font-semibold text-white">Usuário</Text>
    </View>
  );
}
